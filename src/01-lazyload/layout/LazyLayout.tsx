import { Navigation } from "../routes/Navigation";
const LazyLayout = () => {
  return (
    <div>
      <h1>Lazy layout main page</h1>
      <Navigation />
    </div>
  );
};

export default LazyLayout;
